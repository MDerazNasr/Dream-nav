# 0028 Pseudo-View Renderer

The processing pipeline writes deterministic RGB and depth pseudo-view artifacts from camera-path perturbations so the completion dataset stage can consume concrete files before a full splat renderer is integrated.
