# 0090 Trained Gaussian Command Templates

The bundled trained Gaussian backend now accepts JSON array train and export command templates so DreamNav can drive a real 3DGS engine with explicit workspace placeholders while keeping the worker contract stable and shell-free.
