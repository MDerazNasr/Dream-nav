# 0046 Reconstruction capability signal

DreamNav now exposes a reconstruction capability summary because the upload flow should clearly distinguish a real video-to-3D toolchain from the stub fallback path before users trust the generated scene.

The selection screen surfaces the current frame, pose, and Gaussian backends so placeholder geometry is treated as a setup limitation rather than a hidden product behavior.
