# Nearest View Baseline Panel

The explorer now shows cached model completion beside a nearest-camera baseline summary because the spec requires an honest comparison before claiming the scene-specific model improves over simple lookup.

The baseline tile uses nearest pose metadata until the backend emits rendered nearest-view RGB artifacts.

