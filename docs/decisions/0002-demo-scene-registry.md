# 0002 Demo Scene Registry

DreamNav keeps locked demo scene manifests in `data/scenes` so the API and viewer can be built against validated local contracts before expensive reconstruction and scene-specific model assets are available.
