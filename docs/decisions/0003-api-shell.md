# 0003 API Shell

DreamNav serves scene manifests through FastAPI before wiring the viewer to dynamic data so the browser and future processing jobs share the PDF-defined HTTP boundary instead of reading local files directly.
