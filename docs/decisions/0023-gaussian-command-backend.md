# Gaussian Command Backend

The Gaussian reconstruction stage supports a command backend that must write `splat.ply` because real reconstruction should fail loudly when its browser asset is missing instead of silently falling back to the stub splat.
