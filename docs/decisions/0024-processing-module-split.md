# Processing Module Split

Processing task orchestration stays in `processing_tasks.py` while frame and camera stages live in focused modules because the next backend features need room without turning the task list into a catch-all file.
