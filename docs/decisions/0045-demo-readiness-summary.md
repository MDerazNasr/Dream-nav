# 0045 Demo readiness summary

Locked demo scenes now expose a readiness summary because live presentations need one contract that checks required viewer assets, cached completion fallback assets, quality gate state, and render mode before the scene is opened.

The UI shows the summary on the demo selection screen while the detailed quality report remains inside the explorer.
