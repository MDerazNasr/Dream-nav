# Job Splat Asset

The Gaussian stage writes a job-local `splat.ply` and preserves an existing reconstruction output because the viewer should switch to 3DGS based on the artifact contract, not on a demo scene fallback.
