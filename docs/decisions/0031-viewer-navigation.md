# 0031 Viewer Navigation Controls

The browser viewer uses direct pointer look plus yaw-relative keyboard movement so navigation feels like walking through the reconstructed scene instead of sliding along fixed world axes.
