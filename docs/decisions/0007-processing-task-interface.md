# 0007 Processing Task Interface

DreamNav routes worker stages through explicit task objects so real COLMAP, 3DGS, visibility, and quality-gate commands can replace stub implementations without changing the upload or status API.
