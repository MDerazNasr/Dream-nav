# 0008 Capture Validation Probe

DreamNav keeps capture validation as a filesystem-first probe with optional ffprobe duration lookup so uploads can be assessed on machines without video tooling while still recording richer metadata when ffprobe is available.
