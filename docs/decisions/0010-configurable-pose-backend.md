# 0010 Configurable Pose Backend

DreamNav keeps pose estimation configurable with `stub` as the default so local demos stay reliable while COLMAP or future SLAM backends can be enabled by environment without changing worker code.
