# 0034 Cached Completion Preview

Cached completion previews are served through the web app proxy so quality-gated model outputs can render inside the cross-origin-isolated viewer without weakening browser isolation.
