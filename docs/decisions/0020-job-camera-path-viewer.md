# Job Camera Path Viewer

Completed processing jobs expose a small job-scoped scene bundle instead of mutating the static demo registry because generated reconstruction artifacts should remain tied to their processing job until the full asset pipeline is durable.
