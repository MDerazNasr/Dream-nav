# 0032 Live Camera Pose

The viewer reports a throttled camera pose to React so minimap state, markers, and product metrics stay connected to navigation without forcing a React render on every animation frame.
