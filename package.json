{
  "name": "dream-nav",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "api:dev": ".venv/bin/uvicorn app.main:app --app-dir services/api --host 127.0.0.1 --port 8000",
    "api:install": "python3.12 -m venv .venv && .venv/bin/python -m pip install -r services/api/requirements.txt",
    "api:test": ".venv/bin/python -m pytest services/api",
    "build": "npm run build -w @dream-nav/shared && npm run build -w @dream-nav/scene-registry && npm run build -w @dream-nav/web",
    "lint": "npm run lint --workspaces --if-present",
    "remote-dense:dev": ".venv/bin/uvicorn remote_dense_app.main:app --app-dir services/remote_dense --host 127.0.0.1 --port 8010",
    "remote-dense:image:build": "docker build -f services/remote_dense/image/Dockerfile -t dreamnav/remote-dense-colmap:local .",
    "remote-dense:test": ".venv/bin/python -m pytest services/remote_dense/tests",
    "test": "npm run build -w @dream-nav/shared && npm run build -w @dream-nav/scene-registry && npm run test --workspaces --if-present && npm run api:test && npm run remote-dense:test",
    "typecheck": "npm run typecheck -w @dream-nav/shared && npm run build -w @dream-nav/shared && npm run typecheck -w @dream-nav/scene-registry && npm run build -w @dream-nav/scene-registry && npm run typecheck -w @dream-nav/web",
    "generate:demo-splat": "node scripts/generate-demo-splat.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "24.10.2",
    "typescript": "5.9.3",
    "vitest": "4.0.15"
  }
}
